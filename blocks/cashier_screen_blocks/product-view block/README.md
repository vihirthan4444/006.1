# Product View Block

This is a product view component for the Cashier Screen.

## Build

1. Run `npm install` to install dependencies.
2. Run `npm run build` to bundle the block.
3. The output file will be in `dist/main.js`.

## Usage

Load this block in the POS system by selecting the `dist/main.js` file.
